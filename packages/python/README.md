# ai-toolset (Python)

Python implementation of the single-tool gateway pattern for LLM agents.

See the [top-level README](../../README.md) for full documentation.

Install: `pip install ai-toolset`
Import: `from toolbox import Toolbox, Tool`
